# git-commands

```command
git remote set-url origin new_url
git config --global user.email "amazingandyyy@gmail.com"
git push --set-upstream origin master
ssh-keygen -t rsa -C "your_email@example.com"
ssh-add ~/.ssh/id_rsa
```
